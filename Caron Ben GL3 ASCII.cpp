#include <iostream>
/*
 Ben Caron Period 1
 determine if letter pressed is upper or lower case
*/
using namespace std;

int main()
{
    // ask user to enter a letter
    //char letter; // stores the letter the user says
    int number;


    //ask user for a letter
    cout << "Please enter a letter:" << endl;
    cin >> number;
    cin.get();

    //print out ASCII code for that letter
    cout <<"The letter representation of your ASCII code is " << char(number);

    cin.get();
    while (number=7);
         cout <<"The letter representation of your ASCII code is " << char(number);
    return 0;
}
